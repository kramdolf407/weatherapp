sudo: required
language: python
python:
  - "3.6"

services:
  - docker

install:
  - pip install -r requirements.txt

script:
  - docker build -t backend1 ./backend
  - docker run -p 9000:9000 backend1
  - python3 ./backend/tests/request_to_backend.py
  - docker stop $(docker ps -a -q)
